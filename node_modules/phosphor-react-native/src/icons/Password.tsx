/* GENERATED FILE */
import { type IconProps } from 'phosphor-react-native'

import IconBase from "../lib/icon-base";
import weights from '../defs/Password'

function Password({...props }: IconProps) {
  return (<IconBase {...props} weights={weights} name="password" />)
}

export default Password