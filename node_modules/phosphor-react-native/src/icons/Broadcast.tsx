/* GENERATED FILE */
import { type IconProps } from 'phosphor-react-native'

import IconBase from "../lib/icon-base";
import weights from '../defs/Broadcast'

function Broadcast({...props }: IconProps) {
  return (<IconBase {...props} weights={weights} name="broadcast" />)
}

export default Broadcast