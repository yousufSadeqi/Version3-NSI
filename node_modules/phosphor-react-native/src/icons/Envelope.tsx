/* GENERATED FILE */
import { type IconProps } from 'phosphor-react-native'

import IconBase from "../lib/icon-base";
import weights from '../defs/Envelope'

function Envelope({...props }: IconProps) {
  return (<IconBase {...props} weights={weights} name="envelope" />)
}

export default Envelope