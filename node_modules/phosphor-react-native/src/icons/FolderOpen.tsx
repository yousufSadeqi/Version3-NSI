/* GENERATED FILE */
import { type IconProps } from 'phosphor-react-native'

import IconBase from "../lib/icon-base";
import weights from '../defs/FolderOpen'

function FolderOpen({...props }: IconProps) {
  return (<IconBase {...props} weights={weights} name="folder-open" />)
}

export default FolderOpen