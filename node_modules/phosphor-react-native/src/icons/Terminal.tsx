/* GENERATED FILE */
import { type IconProps } from 'phosphor-react-native'

import IconBase from "../lib/icon-base";
import weights from '../defs/Terminal'

function Terminal({...props }: IconProps) {
  return (<IconBase {...props} weights={weights} name="terminal" />)
}

export default Terminal