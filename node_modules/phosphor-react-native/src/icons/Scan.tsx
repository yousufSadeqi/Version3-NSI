/* GENERATED FILE */
import { type IconProps } from 'phosphor-react-native'

import IconBase from "../lib/icon-base";
import weights from '../defs/Scan'

function Scan({...props }: IconProps) {
  return (<IconBase {...props} weights={weights} name="scan" />)
}

export default Scan