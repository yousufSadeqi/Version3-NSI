/* GENERATED FILE */
import { type IconProps } from 'phosphor-react-native'

import IconBase from "../lib/icon-base";
import weights from '../defs/Folder'

function Folder({...props }: IconProps) {
  return (<IconBase {...props} weights={weights} name="folder" />)
}

export default Folder