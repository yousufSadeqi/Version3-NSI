/* GENERATED FILE */
import { type IconProps } from 'phosphor-react-native'

import IconBase from "../lib/icon-base";
import weights from '../defs/Bluetooth'

function Bluetooth({...props }: IconProps) {
  return (<IconBase {...props} weights={weights} name="bluetooth" />)
}

export default Bluetooth